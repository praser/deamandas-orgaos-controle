import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

import styled from 'styled-components';

const HighlightIcon = styled(FontAwesomeIcon)`
  font-size: 32px;
  font-weight: 900;
  color: rgb(221, 223, 235);
  margin-left: 8px;
`;

export default HighlightIcon;
