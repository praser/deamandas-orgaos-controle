import styled from 'styled-components';

import Username from './Username';

const UserLotation = styled(Username)`
  font-size: 9px;
  font-weight: 300;
  line-height: 6px;
`;

export default UserLotation;
